{
  "name": "agentscope",
  "version": "0.0.1",
  "description": "Agent-traffic log analyzer — MVP stage 1: log parser",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:part1": "vitest run tests/parser.part1.test.ts",
    "test:part2": "vitest run tests/parser.part2.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
